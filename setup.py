from setuptools import setup, find_packages

# for installing data_structures, examples and algorithms directory as packages
setup(name='data_structures', version='1.0', packages=find_packages())
setup(name='algorithms', version='1.0', packages=find_packages())
setup(name='examples', version='1.0', packages=find_packages())
